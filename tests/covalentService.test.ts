import { describe, it, expect } from 'vitest';

// Legacy Covalent tests removed. Keep a placeholder suite to avoid CI failures while migrating to Bitquery.
describe.skip('covalentService (deprecated)', () => {
  it('deprecated placeholder', () => {
    expect(true).toBe(true);
  });
});
